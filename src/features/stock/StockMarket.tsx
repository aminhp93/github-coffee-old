import * as React from 'react';

export interface IStockMarketProps {}

export default function StockMarket(props: IStockMarketProps) {
  return (
    <div>
      StockMarket
      <div>
        - Chi so lam phat - Chi so dow jon - Chi so nasdag - Chi so gia dau
        Brent - Tin trong nuoc: -
      </div>
    </div>
  );
}
